

// Tasks Priority wise

- Navigation after registration (on hold)

- increase and decrease quantity not working properly
    - updating the state in localstorage correctly but not on UI
    - needs refresh to update it

- Search functionality 
    - it is printing on colsole if product is there or not
    - requires full title to Search

- different carts for all the users 
    - currently one same cart is being served to all the users
    - aana mate user register karela che e array of object ma bijo sub object add kari devar cart naam no and state thi i think thai jase

- create checkout page (take reference qazi's amazon clone)
    - we can use the same cart array to checkout